{
  "name": "monster-card-server",
  "version": "1.0.0",
  "description": "Backend for Monster Card AI app",
  "main": "server.js",
  "scripts": {
    "build": "cd ../client && npm run build",
    "start": "npm run build && node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
